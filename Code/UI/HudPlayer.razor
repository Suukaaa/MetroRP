@using Sandbox;
@using Sandbox.UI;
@using System.Formats.Asn1
@inherits PanelComponent
@namespace Sandbox

<root>
    <div class=vitals>
        <p>@GetTimeString()</p>
        <p>🪙@playerParam.Money</p>
        <p>@playerParam.Health</p>
        <p>@playerParam.Shield</p>
        <p>@playerParam.Stamina</p>
        <p>@playerParam.Radiation</p>
    </div>
	
</root>

@code
{

    [Property] PlayerParam playerParam { get; set; }

	// Fonction qui retourne
	string GetTimeString()
	{
		var time = playerParam.TimeAlive;
		var minutes = (int)time / 60;
		var secondes=(int)time % 60;
		return $"{minutes:00}:{secondes:00}";
	}
    protected override int BuildHash()
    {
        return System.HashCode.Combine( playerParam.Money, GetTimeString());
    }
}