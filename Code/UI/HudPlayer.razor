@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent
@namespace Sandbox

<root>
	<div class=vitals>
		<p>🪙@Param.GetMoney()</p>
		<p></p>
		<p></p>
		<p></p>

	</div>
</root>

@code
{

	[Property] PlayerParam Param { get; set; }
	protected override int BuildHash()
	{
		return System.HashCode.Combine( Param.GetMoney() );
	}
}